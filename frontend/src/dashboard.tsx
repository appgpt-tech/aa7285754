import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "notes" />
      <CardContent> keeps notes of things < /CardContent>
      </Card>
    );