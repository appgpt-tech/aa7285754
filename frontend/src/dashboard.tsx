import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "BookStoreApp" />
      <CardContent> An application to manage a book store inventory < /CardContent>
      </Card>
    );