import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "Book app" />
      <CardContent> An application that manages books, authors and genres. < /CardContent>
      </Card>
    );