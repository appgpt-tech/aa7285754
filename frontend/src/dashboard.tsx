import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "Project Mgmt" />
      <CardContent> Project Mgmt app for managing different projects < /CardContent>
      </Card>
    );