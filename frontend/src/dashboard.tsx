import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "LibraryManagement" />
      <CardContent> An application for managing library resources. < /CardContent>
      </Card>
    );